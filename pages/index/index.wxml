<scroll-view scroll-x class="newsType">
  <view class="newsTypeList">
    <view class="newsTypeText" wx:for="{{newsTypeList}}" data-id="{{item.id}}" style='color:{{item.color}}' bindtap='selNewsType'>
      {{item.text}}
    </view>
  </view>
</scroll-view>